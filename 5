1. Дан массив из п целых чисел (п≤ 20). Написать программу, кото- рая заменяет в данном массиве все отрицательные элементы нулями.
#include <iostream>
using namespace std;
int main()
{
int a[20]; // объявляем статический массив,
int n;
// задав максимальную размерность
cout << "n = "
cin >> n; // ввели количество элементов массива
for (int i = 0; i < n; ++i) // ввод и обработка данных
{
cout << "a" << i << "] = ";
cin >> a[i];
if (a[i] < 0) [1] = 0;
}
for (int i
=
// ввод очередного элемента
// если і-й элемент массива отрицательный, // заменяем его нулем
0;i < n; ++i) // вывод массива на экран cout << a[i] << "\t";
return 0;
}
